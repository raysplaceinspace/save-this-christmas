pub fn obstacle.ChristmasTree(pos) {
    use body=obstacle
    use radius=TileRadius
    
    Body(pos=, immovable=true, noRotation=true)
    PolygonCollider(shape=Circle, category=Category:Crate)
    PolygonSprite(shape=Equilateral(numPoints=4, angle=0.125rev), color=#0f3)
}