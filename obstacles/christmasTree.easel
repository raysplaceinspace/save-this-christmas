pub fn obstacle.ChristmasTree(pos) {
    use body=obstacle
    use radius=TileRadius, shape=Equilateral(numPoints=4, angle=0.125rev)
    Body(pos=, immovable=true, noRotation=true)
    PolygonCollider(category=Category:Obstacle)
    PolygonSprite(color=#0f3)
}